<%= form_for([@student, @order]) do |f| %>

        <% @menu.items.each do |menu_item| %>
            <% #f.check_box(menu_item.name, menu_item.id) %>

            <h1>SUCCESS!</h1>
        <% end %>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>